{
  "btcd": [
    {
      "url": "bsvd:${BSV_RPC_PORT}",
      "auth": "${RPC_USER}",
      "pass": "${RPC_PASSWORD}",
      "notify": true
    }
  ],
  "btcaddress": "${PAYOUT_ADDRESS}",
  "btcsig": "/AxeBSV/",
  "mindiff": 1,
  "startdiff": 1,
  "maxdiff": 0,
  "logdir": "/www",
  "webdir": "/www/pool",
  "userdir": "/www/users",
  "zmqblock": "tcp://bsvd:${BSV_ZMQ_HASHBLOCK_PORT}"
}
